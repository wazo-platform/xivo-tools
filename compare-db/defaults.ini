[database_uri]
migrated = postgresql://postgres:secret@127.0.0.1:<port>/asterisk
installed = postgresql://postgres:secret@127.0.0.1:<port>/asterisk

[repos]
manage_db = /<path>/<to>/<repository>/xivo-manage-db
